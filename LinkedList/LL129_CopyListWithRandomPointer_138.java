package DSA.LinkedList;
/*
Question:
You are given a linked list where each node contains a next pointer and a random pointer.
Create a deep copy of the linked list and return the head of the copied list.
Input:
        7 -> 13 -> 11 -> 10 -> 1 -> null
        random pointers may point to another node or null
        1 -> 2 -> null
        random pointers: null, node 1
        null
Example 1:
Output:
A deep copy of the linked list with the same next and random pointer connections.
Explanation:
New nodes must be created without sharing nodes with the original linked list.
Example 2:
Output:
A deep copy with the same random pointer structure.
Explanation:
Both next and random connections must be copied correctly.
Example 3:
Output:
null
Explanation:
An empty linked list produces an empty copy.
--------------------------------------------------
*/
public class LL129_CopyListWithRandomPointer_138 {
}
