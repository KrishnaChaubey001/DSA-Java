package DSA.LinkedList;
/*
Question:
Given the head of a linked list, swap every two adjacent nodes and return the head of the modified linked list.
Input:
        1 -> 2 -> 3 -> 4 -> null
        1 -> 2 -> 3 -> null
        1 -> null
Example 1:
Output:
2 -> 1 -> 4 -> 3 -> null
Explanation:
Every pair of adjacent nodes is swapped.
Example 2:
Output:
2 -> 1 -> 3 -> null
Explanation:
The first two nodes are swapped and the last node remains unchanged.
Example 3:
Output:
1 -> null
Explanation:
There is no adjacent node to swap.
--------------------------------------------------
*/
public class LL123_SwapNodesInPairs_24 {
}
